

###! Rcmdr Options Begin !###
options(Rcmdr=
structure(list(ask.to.exit = TRUE, ask.on.exit = TRUE, quit.R.on.close = FALSE, 
    number.messages = TRUE, retain.messages = TRUE, use.markdown = TRUE, 
    use.knitr = FALSE, log.font.family = "Ubuntu Mono", default.font.family = "Ubuntu", 
    log.font.size = 12, default.font.size = "12", log.width = 120, 
    log.height = 20, log.commands = TRUE, output.height = 20, 
    scientific.notation = 5, console.output = FALSE, default.contrasts = c("contr.Treatment", 
    "contr.poly"), grab.focus = TRUE, double.click = FALSE, sort.names = TRUE, 
    show.edit.button = TRUE, suppress.icon.images = FALSE, retain.selections = TRUE, 
    use.rgl = TRUE, log.text.color = "black", command.text.color = "darkred", 
    output.text.color = "blue4", error.text.color = "red", warning.text.color = "darkgreen", 
    title.color = "gray35", theme = "radiance"), .Names = c("ask.to.exit", 
"ask.on.exit", "quit.R.on.close", "number.messages", "retain.messages", 
"use.markdown", "use.knitr", "log.font.family", "default.font.family", 
"log.font.size", "default.font.size", "log.width", "log.height", 
"log.commands", "output.height", "scientific.notation", "console.output", 
"default.contrasts", "grab.focus", "double.click", "sort.names", 
"show.edit.button", "suppress.icon.images", "retain.selections", 
"use.rgl", "log.text.color", "command.text.color", "output.text.color", 
"error.text.color", "warning.text.color", "title.color", "theme"
))
)

# Uncomment the following 4 lines (remove the #s)
#  to start the R Commander automatically when R starts:

# local({
#    old <- getOption('defaultPackages')
#    options(defaultPackages = c(old, 'Rcmdr'))
# })

###! Rcmdr Options End !###
